# Игра "Ксюша Единорог"

Детская игра для Android про единорога Ксюшу, где нужно бегать и уклоняться от врагов.

## Описание игры

- Единорог Ксюша автоматически бежит вперед
- Нажимайте на левую, центральную или правую часть экрана, чтобы переключаться между дорожками
- Уклоняйтесь от красных врагов
- Набирайте очки за каждого пропущенного врага
- Игра становится сложнее со временем

## Сборка APK файла

> **💡 Нет Android Studio?** Смотрите подробную инструкцию в файле `BUILD_WITHOUT_STUDIO.md` - там описаны все способы сборки без Android Studio!

### Требования:
- Android Studio (последняя версия) **ИЛИ** Android SDK + JDK (см. `BUILD_WITHOUT_STUDIO.md`)
- Android SDK
- JDK 8 или выше

### Инструкция по сборке:

1. **Откройте проект в Android Studio:**
   - Запустите Android Studio
   - Выберите "Open an Existing Project"
   - Укажите папку `d:\app`

2. **Дождитесь синхронизации Gradle:**
   - Android Studio автоматически загрузит все зависимости
   - Дождитесь завершения процесса "Gradle Sync"

3. **Соберите APK:**
   - В меню выберите: `Build` → `Build Bundle(s) / APK(s)` → `Build APK(s)`
   - Или используйте терминал:
     ```
     ./gradlew assembleDebug
     ```
     (на Windows: `gradlew.bat assembleDebug`)

4. **Найдите готовый APK:**
   - Файл будет находиться в: `app/build/outputs/apk/debug/app-debug.apk`

5. **Установка на телефон:**
   - Скопируйте файл `app-debug.apk` на ваш Android телефон
   - Включите "Установка из неизвестных источников" в настройках телефона
   - Откройте APK файл на телефоне и установите

### Альтернативный способ (через командную строку):

Если у вас установлен Android SDK и Gradle:

```bash
# Windows
gradlew.bat assembleDebug

# Linux/Mac
./gradlew assembleDebug
```

APK файл будет в `app/build/outputs/apk/debug/app-debug.apk`

## Минимальные требования

- Android 5.0 (API 21) или выше
- Экран любого размера
- Поддержка сенсорного управления

## Управление

- **Нажмите на левую часть экрана** - переключиться на левую дорожку
- **Нажмите на центральную часть** - переключиться на центральную дорожку
- **Нажмите на правую часть** - переключиться на правую дорожку
- **Нажмите после окончания игры** - начать заново

## Особенности

- Простая и понятная графика
- Плавная анимация
- Прогрессивное увеличение сложности
- Подсчет очков
- Подходит для детей

Приятной игры! 🦄

