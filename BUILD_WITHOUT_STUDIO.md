# Сборка APK без Android Studio на Windows 11

## Способ 1: Установка Android SDK и Gradle (рекомендуется)

> **💡 Быстрая проверка:** Запустите скрипт `setup-build-env.ps1` для автоматической проверки и настройки окружения!

### Шаг 1: Установка Java JDK

1. Скачайте JDK 11 или выше с официального сайта Oracle:
   - https://www.oracle.com/java/technologies/downloads/
   - Или используйте OpenJDK: https://adoptium.net/

2. Установите JDK и добавьте в PATH:
   - После установки откройте "Переменные среды" (через поиск Windows)
   - Добавьте в PATH путь к `bin` папке JDK (например: `C:\Program Files\Java\jdk-11\bin`)
   - Проверьте установку: откройте PowerShell и выполните:
     ```powershell
     java -version
     javac -version
     ```

### Шаг 2: Установка Android SDK (Command Line Tools)

1. Скачайте Android SDK Command Line Tools:
   - Перейдите на https://developer.android.com/studio#command-tools
   - Скачайте "Command line tools only" для Windows
   - Распакуйте архив (например, в `C:\Android\sdk`)

2. Создайте структуру папок:
   ```
   C:\Android\sdk\
   ├── cmdline-tools\
   │   └── latest\
   │       └── (содержимое распакованного архива)
   ```

3. Установите необходимые компоненты SDK:
   ```powershell
   cd C:\Android\sdk\cmdline-tools\latest\bin
   
   # Примите лицензии
   .\sdkmanager --licenses
   
   # Установите необходимые пакеты
   .\sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"
   ```

### Шаг 3: Установка Gradle (опционально, можно использовать wrapper)

**Вариант A: Использовать Gradle Wrapper (уже включен в проект)**
- Просто используйте `gradlew.bat` из папки проекта

**Вариант B: Установить Gradle глобально**
1. Скачайте Gradle с https://gradle.org/releases/
2. Распакуйте в `C:\Gradle`
3. Добавьте `C:\Gradle\gradle-8.0\bin` в PATH

### Шаг 4: Настройка проекта

1. Создайте файл `local.properties` в корне проекта (`d:\app\local.properties`):
   ```properties
   sdk.dir=C\:\\Android\\sdk
   ```
   (Замените путь на ваш реальный путь к Android SDK)

2. Проверьте, что файл `gradlew.bat` существует в корне проекта

### Шаг 5: Сборка APK

Откройте PowerShell или командную строку в папке проекта:

```powershell
cd d:\app
.\gradlew.bat assembleDebug
```

Если Gradle Wrapper не работает, используйте глобальный Gradle:
```powershell
gradle assembleDebug
```

### Шаг 6: Найти готовый APK

После успешной сборки APK будет здесь:
```
d:\app\app\build\outputs\apk\debug\app-debug.apk
```

---

## Способ 2: Использование Docker (для продвинутых пользователей)

Если у вас установлен Docker Desktop:

1. Создайте Dockerfile в корне проекта:
```dockerfile
FROM gradle:8.0-jdk11
WORKDIR /app
COPY . .
RUN gradle assembleDebug
```

2. Соберите и запустите:
```powershell
docker build -t android-build .
docker run -v ${PWD}/app/build/outputs:/output android-build
```

---

## Способ 3: Онлайн-сервисы (самый простой способ)

### Использование GitHub Actions (бесплатно)

1. Создайте аккаунт на GitHub (если нет)
2. Загрузите проект в репозиторий
3. Создайте файл `.github/workflows/build.yml`:

```yaml
name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Build APK
        run: ./gradlew assembleDebug
      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/app-debug.apk
```

4. Загрузите код на GitHub
5. Перейдите в Actions → Build APK → Run workflow
6. После завершения скачайте APK из артефактов

---

## Способ 4: Упрощенная установка через Chocolatey

Если у вас установлен Chocolatey (менеджер пакетов для Windows):

```powershell
# Установите Java
choco install openjdk11

# Установите Android SDK (требует ручной настройки)
# Скачайте вручную с developer.android.com

# Установите Gradle
choco install gradle
```

---

## Решение проблем

### Ошибка "SDK location not found"
Создайте файл `local.properties` в корне проекта:
```properties
sdk.dir=C\:\\Android\\sdk
```
(Замените на ваш путь)

### Ошибка "Java not found"
- Установите JDK 11 или выше
- Добавьте Java в PATH
- Проверьте: `java -version`

### Ошибка "Gradle wrapper not found"
Убедитесь, что файл `gradlew.bat` существует в корне проекта. Если нет, скачайте его:
```powershell
# Или создайте проект заново через Android Studio один раз
```

### Ошибка при загрузке зависимостей
- Проверьте подключение к интернету
- Gradle загружает зависимости при первой сборке
- Это может занять 5-10 минут

### Ошибка "SDK platform not found"
Установите необходимые платформы:
```powershell
cd C:\Android\sdk\cmdline-tools\latest\bin
.\sdkmanager "platforms;android-34" "build-tools;34.0.0"
```

---

## Быстрая проверка готовности

Выполните в PowerShell в папке проекта:

```powershell
# Проверка Java
java -version

# Проверка Gradle (если установлен глобально)
gradle -version

# Проверка Android SDK (если добавлен в PATH)
adb version
```

---

## Минимальные требования для сборки

- **Java JDK 11+** (обязательно)
- **Android SDK** с платформой Android 34 и Build Tools 34.0.0
- **Gradle 8.0+** (или используйте wrapper из проекта)
- **Интернет-соединение** (для загрузки зависимостей при первой сборке)
- **~2 ГБ свободного места** (для SDK и зависимостей)

---

## Рекомендация

**Самый простой способ для Windows 11:**
1. Установите JDK 11
2. Установите Android SDK Command Line Tools
3. Создайте `local.properties` с путем к SDK
4. Запустите `gradlew.bat assembleDebug`

Или используйте **GitHub Actions** - это самый простой способ без установки чего-либо на компьютер!

